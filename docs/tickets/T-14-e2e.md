# T-14 E2E 與跨工具測試

## 目標
plan.md 第 5 節的第 2、3、4 層。

## 範圍
- 第 2 層:`crates/cli/tests/e2e.rs`,建 fixture repo → `snip copy --stdout` → `snip paste --stdin --apply` 到空目錄 / 另一個 clone → 比對。檔案模式與 commit 模式都要。
- 第 3 層:用 `.ts-ref` 的 TS 版(`node --experimental-strip-types`)對同一 repo 產生 payload,與 Rust 版逐位元組比對;Rust 還原 TS 產生的 payload。只在有 node 時執行。
- 第 4 層:寫入系統剪貼簿再讀回(Unicode、1 MB、CRLF),沒有顯示環境時跳過。

## 只可修改
`crates/cli/tests/**`、`crates/core/tests/clipboard_roundtrip.rs`、`scripts/**`。

## 驗收
`cargo test --workspace` 全綠(沒有 node 或顯示環境時相關測試明確跳過並印出原因)。
